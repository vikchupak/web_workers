{
  "name": "service-worker",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --open=index.html --port=3256"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
